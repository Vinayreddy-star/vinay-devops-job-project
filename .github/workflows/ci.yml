name: Docker CI/CD
on: [push, pull_request]
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build & Test
      run: |
        docker build -t app .
        docker run --rm -p 3000:3000 app & sleep 3 && curl -f http://localhost:3000/health
